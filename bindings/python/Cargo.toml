[package]
name = "cross_logger_python"
version = "0.1.0"
edition = "2024"

[lib]
name = "cross_logger"
crate-type = ["cdylib"]

[dependencies]
cross_logger_core = { package = "core", path = "../../core" }
pyo3 = { version = "0.23", features = ["extension-module", "abi3-py39"] }
